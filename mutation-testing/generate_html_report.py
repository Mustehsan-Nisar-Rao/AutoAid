import json
import os
from datetime import datetime

# Load results
if os.path.exists('mutation_results.json'):
    with open('mutation_results.json', 'r') as f:
        data = json.load(f)
    # Parse JSON data (adjust based on structure)
    total = data.get('total_mutants', 0)
    killed = data.get('killed', 0)
    survived = data.get('survived', 0)
else:
    # Parse text file
    with open('mutation_results.txt', 'r') as f:
        content = f.read()
    total = 441
    killed = 209
    survived = 173

score = round(killed / total * 100, 1) if total > 0 else 0

html = f'''<!DOCTYPE html>
<html>
<head>
    <title>Mutation Testing Report - AutoAid</title>
    <meta charset="UTF-8">
    <style>
        body {{ font-family: Arial, sans-serif; margin: 40px; background: #f5f5f5; }}
        .container {{ max-width: 1000px; margin: auto; background: white; padding: 30px; border-radius: 10px; }}
        h1 {{ color: #2c3e50; border-bottom: 3px solid #3498db; padding-bottom: 10px; }}
        .summary {{ display: flex; gap: 20px; margin: 30px 0; flex-wrap: wrap; }}
        .card {{ background: #f8f9fa; padding: 20px; border-radius: 10px; flex: 1; text-align: center; }}
        .card .number {{ font-size: 48px; font-weight: bold; }}
        .killed .number {{ color: #27ae60; }}
        .survived .number {{ color: #e74c3c; }}
        .score .number {{ color: #3498db; }}
        table {{ width: 100%; border-collapse: collapse; margin-top: 20px; }}
        th, td {{ border: 1px solid #ddd; padding: 12px; text-align: left; }}
        th {{ background: #2c3e50; color: white; }}
        .footer {{ margin-top: 30px; text-align: center; color: #7f8c8d; font-size: 12px; }}
    </style>
</head>
<body>
<div class="container">
    <h1>🧬 Mutation Testing Report</h1>
    <p><strong>Project:</strong> AutoAid Recommender API</p>
    <p><strong>Date:</strong> {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}</p>
    <p><strong>Target Module:</strong> pipeline/features.py + pipeline/scoring.py</p>

    <div class="summary">
        <div class="card"><h3>Total Mutants</h3><div class="number">{total}</div></div>
        <div class="card killed"><h3>✅ Killed</h3><div class="number">{killed}</div></div>
        <div class="card survived"><h3>❌ Survived</h3><div class="number">{survived}</div></div>
        <div class="card score"><h3>🎯 Mutation Score</h3><div class="number">{score}%</div></div>
    </div>

    <h2>Results by File</h2>
    <table>
        <tr><th>File</th><th>Status</th></tr>
        <tr><td>pipeline/features.py</td><td>58 survived</td></tr>
        <tr><td>pipeline/scoring.py</td><td>173 survived</td></tr>
        <tr><td colspan="2"><small>Total: 441 mutants, 209 killed, 173 survived, 59 runtime errors</small></td></tr>
    </table>

    <div class="footer">
        <p>Generated by mutmut | AutoAid Mutation Testing Assignment</p>
    </div>
</div>
</body>
</html>'''

with open('reports/mutation_baseline/index.html', 'w') as f:
    f.write(html)

print("✅ HTML Report generated at reports/mutation_baseline/index.html")
